run:
  timeout: 5m
  # Go version is read from go.mod (1.26). CI builds golangci-lint from source
  # with the runner's matching toolchain (install-mode: goinstall) so its
  # type-checker understands 1.26 — a prebuilt binary lags and rejects it.

linters:
  enable:
    - gosimple       # simplification suggestions
    - govet          # vet checks (shadow, printf, etc.)
    - ineffassign    # assignments to variables never used after assignment
    - staticcheck    # staticcheck suite
    - unused         # unused code
    - gofmt          # formatting
    - goimports      # import ordering
    - misspell       # common english misspellings in comments/strings
    - unconvert      # remove unnecessary type conversions

linters-settings:
  govet:
    enable-all: true
    disable:
      - fieldalignment   # struct padding — not worth the noise at this stage
  goimports:
    local-prefixes: enterprise-llm-tracker

issues:
  exclude-rules: []
